{
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "cd web && npm start"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21"
  }
}
